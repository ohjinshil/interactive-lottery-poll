# PRD: 디지털 AI 능력 UP! 복권 투표 웹앱

## 1. 목표

카카오톡 같은 모바일 소셜 채널에서 링크 하나로 참여하는 이벤트 투표 웹앱을 만든다. 참여자는 선택지에 투표한 뒤 복권을 받고, 마감 후 전체 참여자 중 1명이 리워드 이미지를 확인한다.

## 2. 배포 방식

- 프론트엔드/백엔드: Next.js on Vercel
- 무료 MVP 저장소: Upstash Redis Free
- 로컬 개발 저장소: `data/local-store.json`
- 관리자 화면: `/admin`
- 참여자 화면: `/`

## 3. 핵심 사용자 경험

- 모바일 앱 같은 다크 테마 화면
- 이미지 썸네일이 크게 들어가는 카드형 선택지
- 카드 위에 투표율, 참여자 수, 상황 태그 오버레이
- 실시간 남은 시간 표시
- 투표 완료 즉시 복권 화면으로 전환
- 하단 고정 플로팅 버튼
- Web Share API 기반 공유, 미지원 환경에서는 링크 복사

## 4. 관리자 기능

- 투표 제목, 설명, 진행 시간, 호스트명, 태그 입력
- 2개 선택지 이름과 이미지 등록
- 당첨 리워드 이미지 등록
- `ADMIN_PIN` 환경변수 설정 시 PIN 보호
- 생성 완료 후 공유 URL 표시

## 5. 참여자 기능

- 브라우저 기기 ID를 `localStorage`에 저장
- 1기기 1투표 제한
- 투표 후 수정 불가
- 마감 전에는 복권 결과 확인 비활성화
- 마감 후 결과 확인 가능

## 6. 복권 및 보안

- 공개 투표 API는 리워드 이미지를 절대 반환하지 않는다.
- 리워드 이미지는 당첨자가 결과 확인을 요청하는 순간에만 반환한다.
- Redis 배포 모드에서는 기기별 투표 키와 선택지별 카운터를 사용해 동시 투표 덮어쓰기 위험을 줄인다.
- 최초 결과 확인 시 당첨자를 뽑아 고정한다.

## 7. MVP 제한

- 이미지는 별도 이미지 CDN이 아니라 데이터 URL로 저장한다.
- Vercel 실배포에서는 Upstash Redis 환경변수가 필요하다.
- 대규모 이벤트나 큰 리워드 이미지는 Vercel Blob, Cloudinary, S3 같은 이미지 저장소로 분리하는 것이 좋다.
